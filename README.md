# CDS HR System - ระบบบริหารจัดการทรัพยากรบุคคล

ระบบ HRMS (Human Resource Management System) แบบ Full-stack สำหรับจัดการข้อมูลพนักงาน การเข้างาน และคำนวณเงินเดือน

## 🎯 Features

- **การจัดการพนักงาน (Employee Management)**
  - เพิ่ม/แก้ไข/ลบข้อมูลพนักงาน
  - อัพโหลดรูปโปรไฟล์และสัญญา PDF
  - กรองและค้นหาพนักงาน
  - สถิติพนักงานตามสถานะและประเภท

- **บันทึกเวลา (Time Attendance)**
  - บันทึกเวลาเข้า-ออกงาน
  - คำนวณเวลาสายอัตโนมัติ
  - บันทึก OT
  - บันทึกการลา (ลาป่วย, ลากิจ, ลาพักร้อน)

- **คำนวณเงินเดือน (Payroll Calculation)**
  - คำนวณเงินรายวัน (สำหรับพนักงานรายวัน)
  - คำนวณเงินรายเดือน (สำหรับพนักงานรายเดือน)
  - หักเงินสายอัตโนมัติ
  - หักเงินลาตามประเภท
  - คำนวณ OT (1.5 เท่า)

- **ตั้งค่าระบบ (System Configuration)**
  - ตั้งค่าค่าปรับสาย
  - ตั้งค่าค่าอาหารและเบี้ยเลี้ยง
  - ตั้งค่าอัตรา OT
  - ตั้งค่าการหักเงินลา

- **รายงาน (Reports)**
  - สรุปการเข้างานรายเดือน
  - พิมพ์สลิปเงินเดือน (PDF)

## 🛠️ เทคโนโลยีที่ใช้

### Backend
- **Node.js** - JavaScript runtime
- **Express** - Web framework
- **PostgreSQL** - Database (pg driver)
- **Multer** - File upload
- **Zod** - Schema validation
- **ES Modules**

### Frontend
- **React 19** - UI library
- **Vite** - Build tool
- **TypeScript** - Type safety
- **Tailwind CSS v4** - Styling
- **Shadcn/UI** - Component library
- **Zustand** - State management
- **React Router v7** - Routing
- **Lucide React** - Icons
- **@react-pdf/renderer** - PDF generation

## 📁 โครงสร้างโปรเจกต์

```
cds-hr-system/
├── backend/                    # Backend API
│   ├── src/
│   │   ├── config/            # Database config
│   │   ├── models/            # Data models
│   │   ├── controllers/       # Business logic
│   │   ├── routes/            # API routes
│   │   ├── middleware/        # Middleware
│   │   ├── utils/             # Utilities
│   │   └── server.js          # Main server
│   ├── uploads/               # Uploaded files
│   └── package.json
│
├── frontend/                   # Frontend App
│   ├── src/
│   │   ├── components/        # React components
│   │   ├── pages/             # Page components
│   │   ├── hooks/             # Custom hooks
│   │   ├── store/             # Zustand store
│   │   ├── types/             # TypeScript types
│   │   ├── lib/               # Utilities
│   │   └── App.tsx            # Main app
│   └── package.json
│
├── database/                   # Database
│   ├── schema.sql             # Database schema
│   └── README.md
│
└── README.md                   # This file
```

## 🚀 การติดตั้งและรัน

### ข้อกำหนด
- Node.js >= 18
- PostgreSQL >= 14
- npm หรือ yarn

### 1. Clone โปรเจกต์

```bash
git clone <repository-url>
cd cds-hr-system
```

### 2. ตั้งค่าฐานข้อมูล

```bash
# สร้าง database
psql -U postgres -h localhost -p 5432
CREATE DATABASE "cds-hr";
\q

# รัน schema
psql -U postgres -h localhost -p 5432 -d cds-hr -f database/schema.sql
```

### 3. ติดตั้ง Backend

```bash
cd backend
npm install

# ตั้งค่า .env (ถ้ายังไม่มี)
cp .env.example .env

# รันเซิร์ฟเวอร์
npm run dev
```

Backend จะรันที่ `http://localhost:5000`

### 4. ติดตั้ง Frontend

```bash
cd frontend
npm install

# ตั้งค่า .env (ถ้ายังไม่มี)
cp .env.example .env

# รันแอพ
npm run dev
```

Frontend จะรันที่ `http://localhost:3000`

## 📊 Business Logic

### การคำนวณเงินเดือน

#### พนักงานรายวัน
```
รายได้รวม = ค่าแรงรายวัน + ค่าอาหาร + เงิน OT
การหัก = ค่าปรับสาย + การหักลา
รายได้สุทธิ = รายได้รวม - การหัก
```

#### พนักงานรายเดือน
```
รายได้รวม = เงินเดือน + เบี้ยเลี้ยง + (ค่าอาหาร × วันที่เข้างาน) + เงิน OT
การหัก = ค่าปรับสาย + การหักลา
รายได้สุทธิ = รายได้รวม - การหัก
```

### การคำนวณสาย
- เปรียบเทียบเวลาเข้างานจริงกับเวลามาตรฐาน (08:00)
- คำนวณจำนวนนาทีที่สาย
- คำนวณค่าปรับ: **นาทีที่สาย × ค่าปรับต่อนาที (default: 2 บาท)**

### การหักเงินลา
- **ลาป่วย**: หัก 0% (ไม่หัก)
- **ลากิจ**: หัก 100% (เต็มจำนวน)
- **ลาพักร้อน**: หัก 0% (ไม่หัก)

### การคำนวณ OT
```
เงิน OT = ชั่วโมง OT × อัตราต่อชั่วโมง × ตัวคูณ OT (1.5)
```

## 📡 API Endpoints

### Employees
- `GET /api/employees` - ดึงพนักงานทั้งหมด
- `GET /api/employees/:id` - ดึงพนักงานตาม ID
- `POST /api/employees` - เพิ่มพนักงาน
- `PATCH /api/employees/:id` - อัพเดทพนักงาน
- `DELETE /api/employees/:id` - ลบพนักงาน

### Attendance
- `GET /api/attendance` - ดึงข้อมูลการเข้างานทั้งหมด
- `GET /api/attendance/employee/:employee_id/:month` - ดึงข้อมูลตามเดือน
- `POST /api/attendance` - สร้างบันทึกการเข้างาน
- `PATCH /api/attendance/:id` - อัพเดทการเข้างาน

### Positions
- `GET /api/positions` - ดึงตำแหน่งทั้งหมด
- `POST /api/positions` - เพิ่มตำแหน่ง

### System Config
- `GET /api/config` - ดึงการตั้งค่าทั้งหมด
- `PATCH /api/config/:key` - อัพเดทการตั้งค่า

## 🔐 การตรวจสอบข้อมูล (Validation)

### เลขบัตรประชาชน
- ต้องมี 13 หลัก
- ตรวจสอบ checksum ตามมาตรฐานไทย

### เบอร์โทรศัพท์
- ต้องมี 10 หลัก
- ต้องเริ่มต้นด้วย 0

### วันที่และเวลา
- วันที่: รูปแบบ YYYY-MM-DD
- เวลา: รูปแบบ HH:MM:SS

## 📤 การอัพโหลดไฟล์

### รูปโปรไฟล์
- รองรับ: JPEG, PNG, GIF, WebP
- ขนาดสูงสุด: 5MB

### ไฟล์สัญญา
- รองรับ: PDF
- ขนาดสูงสุด: 10MB

## 🧪 การทดสอบ

```bash
# Test Backend
curl http://localhost:5000/health
curl http://localhost:5000/api/employees

# Test Frontend
# เปิดเบราว์เซอร์ไปที่ http://localhost:3000
```

## 📝 TODO

- [ ] เพิ่ม Authentication & Authorization
- [ ] เพิ่มระบบแจ้งเตือน (Notifications)
- [ ] เพิ่มรายงานสรุปเงินเดือนรายเดือน
- [ ] เพิ่ม Export ข้อมูลเป็น Excel
- [ ] เพิ่มระบบ Dashboard สถิติ
- [ ] เพิ่ม Unit Tests

## 👥 ผู้พัฒนา

CDS Development Team

## 📄 License

MIT

---

**หมายเหตุ**: โปรเจกต์นี้สร้างขึ้นเพื่อการศึกษาและใช้งานภายในองค์กร
