# Getting Started - CDS HR System

## 🎯 ขั้นตอนการเริ่มต้นใช้งาน

### 1. ติดตั้งฐานข้อมูล

```bash
# เข้าสู่ PostgreSQL
psql -U postgres -h localhost -p 5432

# สร้างฐานข้อมูล
CREATE DATABASE "cds-hr";
\q

# รัน Schema
psql -U postgres -h localhost -p 5432 -d cds-hr -f database/schema.sql
```

### 2. รัน Backend

```bash
cd backend

# ติดตั้ง dependencies
npm install

# รันเซิร์ฟเวอร์ (development mode)
npm run dev
```

Backend จะรันที่ `http://localhost:5000`

ทดสอบ Backend:
```bash
curl http://localhost:5000/health
curl http://localhost:5000/api/employees
curl http://localhost:5000/api/positions
```

### 3. รัน Frontend

```bash
cd frontend

# ติดตั้ง dependencies
npm install

# รันแอพ (development mode)
npm run dev
```

Frontend จะรันที่ `http://localhost:3000`

## 📊 ข้อมูลทดสอบ

หลังจากรัน schema แล้ว ระบบจะมีข้อมูลตัวอย่าง:

### ตำแหน่งงาน (Positions)
- พนักงานทั่วไป (ค่าอาหาร: 50 บาท)
- หัวหน้างาน (ค่าอาหาร: 70 บาท, เบี้ยเลี้ยง: 1,000 บาท)
- ผู้จัดการ (ค่าอาหาร: 100 บาท, เบี้ยเลี้ยง: 3,000 บาท)

### พนักงาน (Employees)
- นายสมชาย ใจดี (พนักงานรายวัน, ค่าแรง: 350 บาท/วัน)
- นางสาวสมหญิง รักงาน (พนักงานรายเดือน, เงินเดือน: 25,000 บาท)
- นางวิไล มานะ (พนักงานรายเดือน, เงินเดือน: 45,000 บาท)

### การตั้งค่าระบบ (System Configs)
- `late_fine_per_minute`: 2 บาท/นาที
- `ot_rate_multiplier`: 1.5 เท่า
- `leave_personal_deduction`: หัก 100%
- `leave_sick_deduction`: ไม่หัก (0%)
- `work_days_per_month`: 26 วัน

## 🔧 การตั้งค่า Environment Variables

### Backend (.env)
```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cds-hr
DB_SCHEMA=hr_system
PORT=5000
NODE_ENV=development
```

### Frontend (.env)
```env
VITE_API_URL=http://localhost:5000/api
VITE_UPLOAD_URL=http://localhost:5000/uploads
```

## 📝 ขั้นตอนการพัฒนาต่อ

### 1. เพิ่ม Shadcn/UI Components

```bash
cd frontend

# ติดตั้ง shadcn/ui CLI (ถ้ายังไม่มี)
# จากนั้นเพิ่ม components ที่ต้องการ เช่น:
# - Button
# - Input
# - Table
# - Dialog
# - Select
# - Tabs
```

### 2. สร้าง Store (Zustand)

สร้างไฟล์ใน `frontend/src/store/`:
- `employeeStore.ts` - จัดการ state ของพนักงาน
- `attendanceStore.ts` - จัดการ state ของการเข้างาน
- `configStore.ts` - จัดการ state ของการตั้งค่า

### 3. สร้าง Custom Hooks

สร้างไฟล์ใน `frontend/src/hooks/`:
- `useEmployees.ts` - Hook สำหรับจัดการพนักงาน
- `useAttendance.ts` - Hook สำหรับจัดการการเข้างาน
- `usePositions.ts` - Hook สำหรับจัดการตำแหน่ง
- `useConfig.ts` - Hook สำหรับจัดการการตั้งค่า

### 4. สร้าง Pages

สร้างไฟล์ใน `frontend/src/pages/`:
- `Dashboard.tsx` - หน้าแดชบอร์ด
- `Employees.tsx` - หน้าจัดการพนักงาน
- `Attendance.tsx` - หน้าบันทึกเวลา
- `Payroll.tsx` - หน้าคำนวณเงินเดือน
- `Settings.tsx` - หน้าตั้งค่า

### 5. สร้าง Components

สร้างไฟล์ใน `frontend/src/components/`:

#### Layout Components
- `Sidebar.tsx` - เมนูด้านข้าง
- `Header.tsx` - หัวเว็บไซต์
- `MainLayout.tsx` - Layout หลัก

#### Employee Components
- `EmployeeForm.tsx` - ฟอร์มเพิ่ม/แก้ไขพนักงาน
- `EmployeeList.tsx` - รายการพนักงาน
- `EmployeeCard.tsx` - การ์ดแสดงข้อมูลพนักงาน

#### Attendance Components
- `AttendanceTable.tsx` - ตารางบันทึกเวลา
- `AttendanceForm.tsx` - ฟอร์มบันทึกเวลา
- `AttendanceCalendar.tsx` - ปฏิทินการเข้างาน

### 6. สร้าง PDF Template

สร้างไฟล์ใน `frontend/src/pdf/`:
- `PayslipTemplate.tsx` - Template สลิปเงินเดือน

## 🧪 การทดสอบ API

### ทดสอบ Employees API

```bash
# ดึงพนักงานทั้งหมด
curl http://localhost:5000/api/employees

# ดึงพนักงานตาม ID
curl http://localhost:5000/api/employees/1

# เพิ่มพนักงาน
curl -X POST http://localhost:5000/api/employees \
  -H "Content-Type: application/json" \
  -d '{
    "first_name": "ทดสอบ",
    "last_name": "ระบบ",
    "id_card_number": "1234567890123",
    "hired_date": "2024-01-01",
    "position_id": 1,
    "employment_type": "daily",
    "base_salary_or_wage": 350
  }'
```

### ทดสอบ Attendance API

```bash
# บันทึกการเข้างาน
curl -X POST http://localhost:5000/api/attendance \
  -H "Content-Type: application/json" \
  -d '{
    "employee_id": 1,
    "date": "2024-01-13",
    "check_in_time": "08:15:00",
    "check_out_time": "17:00:00",
    "ot_hours": 2
  }'

# ดึงข้อมูลการเข้างานตามเดือน
curl http://localhost:5000/api/attendance/employee/1/2024-01
```

## 🎨 การออกแบบ UI

### สี (Colors)
- Primary: `hsl(222.2 47.4% 11.2%)` - สีหลัก
- Secondary: `hsl(210 40% 96.1%)` - สีรอง
- Destructive: `hsl(0 84.2% 60.2%)` - สีแดง (ลบ/ยกเลิก)
- Muted: `hsl(210 40% 96.1%)` - สีอ่อน

### Typography
- Font: ใช้ font ระบบ (system font)
- Heading: `text-2xl`, `text-3xl`, `text-4xl`
- Body: `text-sm`, `text-base`

### เมนู (Menu Items)
- 📊 Dashboard - แดชบอร์ด
- 👥 Employees - จัดการพนักงาน
- ⏰ Attendance - บันทึกเวลา
- 💰 Payroll - คำนวณเงินเดือน
- ⚙️ Settings - ตั้งค่าระบบ

## 🐛 การแก้ไขปัญหา (Troubleshooting)

### Backend ไม่สามารถเชื่อมต่อ Database
1. ตรวจสอบว่า PostgreSQL รันอยู่หรือไม่
2. ตรวจสอบ credentials ใน `.env`
3. ตรวจสอบว่าได้สร้าง database แล้วหรือยัง

### Frontend ไม่สามารถเรียก API
1. ตรวจสอบว่า Backend รันอยู่หรือไม่
2. ตรวจสอบ `VITE_API_URL` ใน `.env`
3. ตรวจสอบ CORS settings ใน Backend

### File Upload ไม่ทำงาน
1. ตรวจสอบว่าโฟลเดอร์ `backend/uploads/` มีอยู่
2. ตรวจสอบ permissions ของโฟลเดอร์
3. ตรวจสอบขนาดไฟล์ (สูงสุด 10MB)

## 📚 เอกสารเพิ่มเติม

- [Backend README](backend/README.md)
- [Database Schema](database/README.md)
- [Project Structure](PROJECT_STRUCTURE.md)

## 💬 การสนับสนุน

หากพบปัญหาหรือมีคำถาม กรุณาติดต่อทีมพัฒนา CDS
